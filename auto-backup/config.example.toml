# Copying this file is not sufficient for scheduled backups.
# Run ./auto-backup/configure.sh to discover available reviewers and models.

[backup]
mode = "device-only"
rebase = true

[review]
enabled = false
reviewers = []

[review.models]

# Generated configs add one table per configurable reviewer and one entry per model.
# Choices are model-specific. AGY encodes effort in its model IDs; experimental
# reviewers keep their provider defaults.
[review.reasoning]
# [review.reasoning.claude]
# opus = "high"
# [review.reasoning.codex]
# "gpt-5.6-sol" = "high"
# [review.reasoning.opencode]
# "opencode/muse-spark-1.2-contributor-free" = "high"
# [review.reasoning.opencode-go-api]
# "opencode-go/deepseek-v4-flash" = "max"
